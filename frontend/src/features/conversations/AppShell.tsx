import { Gamepad2, LogOut, MessageCircle, Plus, Settings, UserRound } from 'lucide-react';
import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { useAuthStore } from '../auth/auth-store';
import { cn } from '../../shared/ui/cn';
export function AppShell() { const signOut=useAuthStore(s=>s.signOut); const navigate=useNavigate(); const links=[['/conversations',MessageCircle,'Conversas'],['/games',Gamepad2,'Jogos'],['/profile',UserRound,'Perfil'] ] as const; return <main className="grid min-h-dvh bg-gloopy-bg-dark md:grid-cols-[84px_1fr]"><aside className="fixed inset-x-0 bottom-0 z-50 flex h-16 items-center justify-around border-t bg-gloopy-surface md:inset-y-0 md:left-0 md:h-auto md:w-[84px] md:flex-col md:justify-start md:gap-3 md:border-r md:border-t-0 md:py-5"><img src="/mascot/gloopy-cool-icon.png" alt="Gloopy" className="hidden h-11 w-11 rounded-xl md:mb-6 md:block" />{links.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>cn('grid min-h-12 min-w-16 place-items-center rounded-xl text-xs font-semibold text-gloopy-muted transition hover:bg-white/10 hover:text-white',isActive&&'bg-gloopy-primary/15 text-gloopy-primary')}><Icon className="h-5 w-5"/><span className="md:sr-only">{label}</span></NavLink>)}<button onClick={()=>navigate('/conversations/new')} className="grid min-h-12 min-w-16 place-items-center rounded-xl text-gloopy-accent md:mt-auto" aria-label="Nova conversa"><Plus /></button><button onClick={()=>{signOut();navigate('/login')}} className="grid min-h-12 min-w-16 place-items-center rounded-xl text-gloopy-muted" aria-label="Sair"><LogOut /></button></aside><div className="min-w-0 pb-16 md:col-start-2 md:pb-0"><Outlet /></div></main>; }
export function PlaceholderPage({title,description}:{title:string;description:string}) { return <section className="flex min-h-[calc(100dvh-4rem)] items-center justify-center p-6 md:min-h-dvh"><div className="max-w-md rounded-3xl border bg-gloopy-surface p-8 text-center"><Settings className="mx-auto h-10 w-10 text-gloopy-primary"/><h1 className="mt-4 font-display text-3xl font-bold">{title}</h1><p className="mt-3 text-gloopy-muted">{description}</p></div></section>; }
